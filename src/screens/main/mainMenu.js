/**
 * @author crazyh / https://github.com/crazyh2
 */

import { Screen } from "../screen.js";

class MainMenu extends Screen {
    constructor(root) {
        super(root);

        this.root = root;
    };

    render(renderGroup) {

    };
};

export { MainMenu };
/**
 * @author crazyh / https://github.com/crazyh2
 */

import { Screen } from "../screen";

class AppsMenu extends Screen {
    constructor(root) {
        super(root);

        this.root = root;
    };

    render(renderGroup) {

    };
};

export { AppsMenu };